export const useSound = () => {
    // Sound removed as per user request
    const playHover = () => { };
    const playClick = () => { };
    const playShockwave = () => { };

    return { playHover, playClick, playShockwave };
};
